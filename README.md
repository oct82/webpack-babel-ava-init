# webpack-babel-ava-init

### Setup

```bash
git clone git@github.com:oct82/webpack-babel-ava-init.git && cd webpack-babel-ava-init
rm -rf .git && git init

npm i
npm start -- --open
# Edit
npm test
npm run publish
```
